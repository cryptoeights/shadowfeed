name = "shadowfeed-facilitator"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

routes = [
  { pattern = "facilitator.shadowfeed.app", custom_domain = true }
]
